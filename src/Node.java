// Represents a node
public class Node {
    public int data;       // 'data' can be any generic type
    public Node next;  // Reference to next node in list

    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}
